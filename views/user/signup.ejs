<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/tailwindcss/output.css" />
    <title>Sign Up | VIRTUE</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />
    <style>
      body {
        background-image: url('/images/Signup.jpg');
        background-size: cover;
        background-position: center;
        min-height: 100vh;
        position: relative;
      }
      
      body::before {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, rgba(0,0,0,0.7) 0%, rgba(0,0,0,0.5) 100%);
      }

      .glass-card {
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(8px);
        border: 1px solid rgba(255, 255, 255, 0.3);
      }

      .input-style {
        background: rgba(255, 255, 255, 0.15);
        border: 1px solid rgba(255, 255, 255, 0.3);
        color: white;
      }

      .input-style:focus {
        background: rgba(255, 255, 255, 0.2);
        border-color: rgba(255, 255, 255, 0.5);
      }

      .input-style::placeholder {
        color: rgba(255, 255, 255, 0.6);
      }
    </style>
  </head>

  <body class="flex items-center justify-center min-h-screen p-4">
    <div class="w-full max-w-[400px] relative z-10">
      <!-- Logo -->
      <div class="text-center mb-8">
        <img src="/images/whitelogo.png" alt="VIRTUE" class="h-14 mx-auto">
      </div>

      <!-- Signup Form -->
      <div class="glass-card rounded-2xl p-8">
        <h1 class="text-2xl font-bold text-white text-center mb-6">Create Account</h1>

        <form id="signup-form" class="space-y-4">
          <!-- First Name -->
          <div class="relative">
            <i class="fas fa-user absolute left-3 top-1/2 -translate-y-1/2 text-white/70"></i>
            <input 
              type="text" 
              id="firstName"
              name="firstName"
              placeholder="First name"
              class="input-style w-full pl-10 pr-4 py-3 rounded-xl outline-none"
              required
            >
          </div>

          <!-- Last Name -->
          <div class="relative">
            <i class="fas fa-user absolute left-3 top-1/2 -translate-y-1/2 text-white/70"></i>
            <input 
              type="text" 
              id="lastName"
              name="lastName"
              placeholder="Last name"
              class="input-style w-full pl-10 pr-4 py-3 rounded-xl outline-none"
              required
            >
          </div>

          <!-- Email -->
          <div class="relative">
            <i class="fas fa-envelope absolute left-3 top-1/2 -translate-y-1/2 text-white/70"></i>
            <input 
              type="email" 
              id="email"
              name="email"
              placeholder="Email address"
              class="input-style w-full pl-10 pr-4 py-3 rounded-xl outline-none"
              required
            >
          </div>

          <!-- Password -->
          <div class="relative">
            <i class="fas fa-lock absolute left-3 top-1/2 -translate-y-1/2 text-white/70"></i>
            <input 
              type="password" 
              id="password"
              name="password"
              placeholder="Password"
              class="input-style w-full pl-10 pr-4 py-3 rounded-xl outline-none"
              required
            >
          </div>

          <!-- Confirm Password -->
          <div class="relative">
            <i class="fas fa-lock absolute left-3 top-1/2 -translate-y-1/2 text-white/70"></i>
            <input 
              type="password" 
              id="confirmPassword"
              name="confirmPassword"
              placeholder="Confirm password"
              class="input-style w-full pl-10 pr-4 py-3 rounded-xl outline-none"
              required
            >
          </div>

          <!-- Error Message -->
          <div id="generalError" class="hidden text-red-400 text-sm text-center"></div>

          <!-- Sign Up Button -->
          <button 
            type="submit"
            class="w-full bg-white text-black font-semibold py-3 rounded-xl hover:bg-opacity-90 transition-all"
          >
            Create Account
          </button>

          <!-- Google Signup -->
          <a href="/auth/google?trigger=signup" 
             class="w-full flex items-center justify-center gap-2 py-3 mt-3 rounded-xl border border-white/30 text-white hover:bg-white/10 transition-all">
            <i class="fab fa-google"></i>
            <span>Continue with Google</span>
          </a>

          <!-- Login Link -->
          <p class="text-center text-white/70 mt-6">
            Already have an account? 
            <a href="/login" class="text-white hover:underline">Sign in</a>
          </p>
        </form>
      </div>
    </div>

    <!-- Loading Spinner -->
    <div id="loading-spinner" class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center">
      <div class="bg-white/10 backdrop-blur p-4 rounded-lg flex items-center gap-3">
        <div class="w-5 h-5 border-2 border-white/30 border-t-white rounded-full animate-spin"></div>
        <span class="text-white">Creating account...</span>
      </div>
    </div>

    <!-- OTP Modal -->
    <div id="otpModal" class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
      <div class="glass-card p-8 rounded-2xl max-w-md w-full mx-4">
        <h2 class="text-2xl font-bold text-white text-center mb-4">Enter OTP</h2>
        <p class="text-white/70 text-center mb-6">Please enter the OTP sent to your email</p>
        
        <div class="flex gap-2 mb-6 justify-center">
          <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-2xl rounded-xl">
          <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-2xl rounded-xl">
          <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-2xl rounded-xl">
          <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-2xl rounded-xl">
          <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-2xl rounded-xl">
          <input type="text" maxlength="1" class="otp-input w-12 h-12 text-center text-2xl rounded-xl">
        </div>
        
        <div id="otpError" class="hidden text-red-400 text-sm text-center mb-4"></div>
        
        <button 
          id="verifyOtp"
          class="w-full bg-white text-black font-semibold py-3 rounded-xl hover:bg-opacity-90 transition-all"
        >
          Verify OTP
        </button>
      </div>
    </div>

    <!-- Referral Modal -->
    <div id="referralModal" class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
      <div class="glass-card p-8 rounded-2xl max-w-md w-full mx-4">
        <h2 class="text-2xl font-bold text-white text-center mb-4">Enter Referral Code</h2>
        <p class="text-white/70 text-center mb-6">Got a referral code? Enter it below to get ₹50 in your wallet!</p>
        
        <div class="mb-6">
          <div class="relative">
            <i class="fas fa-gift absolute left-3 top-1/2 -translate-y-1/2 text-white/70"></i>
            <input 
              type="text" 
              id="referralCodeInput"
              placeholder="Enter referral code"
              class="input-style w-full pl-10 pr-4 py-3 rounded-xl outline-none uppercase"
              maxlength="6"
            >
          </div>
          <div id="referralError" class="hidden text-red-400 text-sm mt-2 text-center"></div>
        </div>
        
        <button 
          type="button"
          id="applyReferral" 
          class="w-full bg-white text-black font-semibold py-3 rounded-xl hover:bg-opacity-90 transition-all mb-4"
        >
          Apply Code
        </button>
        
        <button 
          type="button"
          id="skipReferral" 
          class="w-full border border-white/30 text-white py-3 rounded-xl hover:bg-white/10 transition-all"
        >
          Skip for Now
        </button>
      </div>
    </div>

    <script src="/javascript/user/signup.js"></script>
  </body>
</html>
